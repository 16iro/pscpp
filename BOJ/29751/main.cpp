#include <bits/stdc++.h>
#include <iomanip>
using namespace std;

void solve() {
    float W, H;
    cin >> W >> H;
    cout << fixed << setprecision(1) << (W * H / 2.0);
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    solve();

    return 0;
}
