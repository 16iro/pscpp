#include <bits/stdc++.h>
using namespace std;

void solve() {
    long long N, M;
    cin >> N >> M;
    if (N == M)
        cout << 1;
    else
        cout << 0;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    solve();

    return 0;
}
